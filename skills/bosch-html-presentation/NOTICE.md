# Notice

This custom skill is an original Bosch-oriented composition informed by public Agent Skills patterns, including:

- Anthropic `frontend-design`: emphasis on intentional, subject-specific visual direction and avoiding generic AI-generated design tropes.
- Anthropic `web-artifacts-builder`: the concept of producing a self-contained HTML artifact, while this custom skill deliberately removes its React/TypeScript/Vite/Tailwind toolchain.
- thedesignproject `presentation`: the general single-file HTML presentation model with navigation, progressive reveal, animation, and portable delivery.

The instructions and implementation in this folder are newly written for this skill rather than copied verbatim from those projects.

The bundled `assets/bosch-logo.svg` is preserved unchanged from the supplied skill package. Its corporate approval status has not been independently verified. Every output must reuse this exact asset. Do not replace or redraw it unless the user explicitly supplies or requests a replacement.
